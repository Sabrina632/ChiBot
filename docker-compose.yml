services:
  chibot:
    build: .
    image: chibot:latest
    container_name: chibot
    restart: unless-stopped
    # Monta o config do host dentro do container. Edite o ChiConfig.json na VPS
    # (token, prefixo, GuildId) e reinicie o container — sem precisar rebuildar.
    volumes:
      - ./ChiConfig.json:/app/ChiConfig.json
      # Banco do Party Finder (ChiData.db). Volume nomeado pra persistir entre
      # recriacoes do container (docker compose up --build nao apaga).
      - chibot-data:/app/data
    # Logs ficam acessiveis via "docker logs chibot".
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    # Musica: o bot fala com o Lavalink pela rede interna do compose.
    # No ChiConfig.json da VPS, use "LavalinkUri": "ws://lavalink:2333".
    depends_on:
      - lavalink
      - bgutil-provider
    environment:
      # yt-dlp pede os poTokens (verificacao anti-bot do YouTube) pra ca.
      - YTDLP_POT_PROVIDER=http://bgutil-provider:4416

  # Gera poTokens (resolve o desafio BotGuard do YouTube) pro yt-dlp do bot.
  # Sem isso o YouTube barra IPs de datacenter com "confirm you're not a bot".
  bgutil-provider:
    image: brainicism/bgutil-ytdlp-pot-provider
    container_name: bgutil-provider
    restart: unless-stopped
    init: true
    # Sem "ports": so o bot precisa alcancar o provider.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  lavalink:
    image: ghcr.io/lavalink-devs/lavalink:4
    container_name: lavalink
    restart: unless-stopped
    environment:
      - _JAVA_OPTIONS=-Xmx512M
    volumes:
      - ./lavalink/application.yml:/opt/Lavalink/application.yml
      # Sem volume pros plugins: volume nomeado nasce como root e o Lavalink
      # (uid 322) nao consegue gravar nele. O plugin do YouTube e pequeno e o
      # servidor baixa de novo sozinho quando o container e recriado.
    # Sem "ports": so o bot precisa alcancar o Lavalink, nao a internet.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  chibot-data: