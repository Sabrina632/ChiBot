# Configuracao do servidor Lavalink v4 (audio do ChiBot).
# A senha precisa bater com o "LavalinkPassword" do ChiConfig.json.
server:
  port: 2333
  address: 0.0.0.0

lavalink:
  plugins:
    # Fonte do YouTube mantida como plugin (a embutida foi descontinuada).
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.18.1"
      repository: "https://maven.lavalink.dev/releases"
  server:
    password: "youshallnotpass"
    sources:
      # YouTube embutido desligado: quem cuida disso e o plugin acima.
      youtube: false
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      http: true
      local: false

plugins:
  youtube:
    enabled: true
    allowSearch: true
    allowDirectVideoIds: true
    allowDirectPlaylistIds: true
    # OAuth desligado no node: quem gerencia o login e o BOT (YtOauth), que
    # manda o access token no userData de cada track ({"oauth-token": ...}).
    # Configure o "YoutubeRefreshToken" no ChiConfig.json do bot.
    oauth:
      enabled: false
    # Ordem de clientes usados pra resolver streams/buscas.
    # TVHTML5EMBEDDED e o que carrega videos usando o login do OAuth
    # (os outros nao usam o token e continuam bloqueados em IP de datacenter).
    clients:
      - MUSIC
      - TVHTML5EMBEDDED
      - TV
      - ANDROID_VR
      - WEB
      - WEBEMBEDDED

logging:
  level:
    root: INFO
    lavalink: INFO